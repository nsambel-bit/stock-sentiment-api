[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.install]
cmds = ["pip install -r Sentimentnalysis/requirements.txt"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "gunicorn Sentimentnalysis.app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8 --timeout 0"
